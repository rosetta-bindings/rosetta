*.aux
*.bbl
*.blg
*.log
*.out

# Per-configuration scratch for the experiments: generated libraries, driver
# builds and emitted bindings. Regenerated by each experiment's run.sh; only
# results/ is worth committing.
experimental-eval/*/work/
